document.addEventListener('DOMContentLoaded', function(){ const navbar=document.querySelector('.navbar'); if(navbar) window.addEventListener('scroll', ()=> navbar.classList.toggle('scrolled', window.scrollY>20)); const dots=document.querySelectorAll('.dot'); dots.forEach((d,i)=> d.addEventListener('click', ()=> { document.querySelectorAll('.hero-slide-bg').forEach((b,idx)=> b.style.opacity = (idx===i? '1':'0')); dots.forEach((dd,ii)=> dd.classList.toggle('active', ii===i)); })); setInterval(()=>{ const act = Array.from(dots).findIndex(d=> d.classList.contains('active')); const next = (act+1) % dots.length; dots[next].click(); }, 5000); });